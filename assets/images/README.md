Thư mục này chứa ảnh cho dự án Handora.

- Thêm các file ảnh ở đây (ví dụ: logo.png, product-1.jpg).
- Sử dụng đường dẫn tương đối `/assets/images/<tên-file>` trong code hoặc copy vào `public` nếu cần phục vụ tĩnh.
